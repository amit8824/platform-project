apiVersion: v1 
kind: Service

metadata: 
  name: platform-service

spec:
  type: ClusterIP
  selector:
    app: platform-api
  ports:
  - name: http   
    port: 80
    targetPort: 8000
