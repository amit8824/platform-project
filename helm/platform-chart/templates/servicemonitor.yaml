apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: platform-monitor

  labels:
    release: monitoring

spec:

  selector:
    matchLabels:
      app: platform-api

  endpoints:

  - port: http
    path: /metrics
    interval: 15s
